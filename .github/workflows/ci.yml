name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test
      NEXTAUTH_SECRET: ci-test-secret-not-real
      NEXTAUTH_URL: http://localhost:3000
      NEXT_PUBLIC_SENTRY_DSN: ""
      AWS_REGION: us-east-1
      AWS_ACCESS_KEY_ID: ci-placeholder
      AWS_SECRET_ACCESS_KEY: ci-placeholder
      AWS_S3_BUCKET: ci-placeholder
      STRIPE_SECRET_KEY: sk_test_ci_placeholder
      STRIPE_WEBHOOK_SECRET: whsec_ci_placeholder
      STRIPE_PRICE_STARTER_MONTHLY: price_ci_placeholder
      STRIPE_PRICE_STARTER_ANNUAL: price_ci_placeholder
      STRIPE_PRICE_GROWTH_MONTHLY: price_ci_placeholder
      STRIPE_PRICE_GROWTH_ANNUAL: price_ci_placeholder
      STRIPE_PRICE_PRO_MONTHLY: price_ci_placeholder
      STRIPE_PRICE_PRO_ANNUAL: price_ci_placeholder
      RESEND_API_KEY: re_ci_placeholder
      RESEND_FROM_EMAIL: noreply@ci.local

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm prisma generate

      - name: Run tests
        run: pnpm test:run

      - name: Build
        run: pnpm build
